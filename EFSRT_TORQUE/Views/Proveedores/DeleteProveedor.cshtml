@model EFSRT_TORQUE.Models.Proveedores

@{
    ViewBag.Title = "Eliminar Proveedores";
}

<main>
    <div class="txtTittle">
        <p class="paddistance">Eliminar Proveedores</p>
        <ul>
            <li><ion-icon name="newspaper-outline"></ion-icon></li>
            <li><ion-icon name="notifications-outline"></ion-icon></li>
            <li><ion-icon name="person-outline"></ion-icon></li>
        </ul>
    </div>
    <div>
        <div class="paddistance bannermenu">
            <ul>
                <li><p>Proveedor a Eliminar</p></li>
            </ul>
        </div>
    </div>

    <div class="boxin">
        <div>
            <p>¿Estás seguro de que deseas eliminar este proveedor?</p>

            <dl style="display:flex;" class="texthei">
                <dt class="sontext">
                    @Html.DisplayNameFor(model => model.ProveedorID)
                </dt>
                <dd class="sonbox">
                    @Html.DisplayFor(model => model.ProveedorID)
                </dd>
                <!-- Agrega más propiedades del producto si es necesario -->
            </dl>

            @using (Html.BeginForm("DeleteProveedor", "Proveedores", FormMethod.Post, new { id = "deleteForm" }))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.ProveedorID)

                <div class="form-actions">
                    <input type="submit" value="Eliminar" class="buttons danger" />
                    @Html.ActionLink("Cancelar", "ListarProveedores", null, new { @class = "buttons info" })
                </div>
            }
        </div>
    </div>
</main>
