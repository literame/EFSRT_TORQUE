@model EFSRT_TORQUE.Models.Ventas

@{
    ViewBag.Title = "Eliminar Ventas";
}

<main>
    <div class="txtTittle">
        <p class="paddistance">Eliminar Ventas</p>
        <ul>
            <li><ion-icon name="newspaper-outline"></ion-icon></li>
            <li><ion-icon name="notifications-outline"></ion-icon></li>
            <li><ion-icon name="person-outline"></ion-icon></li>
        </ul>
    </div>
    <div>
        <div class="paddistance bannermenu">
            <ul>
                <li><p>Venta a Eliminar</p></li>
            </ul>
        </div>
    </div>

    <div class="boxin">
        <div>
            <p>¿Estás seguro de que deseas eliminar este venta?</p>

            <dl style="display:flex;" class="texthei">
                <dt class="sontext">
                    @Html.DisplayNameFor(model => model.VentaID)
                </dt>
                <dd class="sonbox">
                    @Html.DisplayFor(model => model.VentaID)
                </dd>
                <!-- Agrega más propiedades del producto si es necesario -->
            </dl>

            @using (Html.BeginForm("DeleteVenta", "Ventas", FormMethod.Post, new { id = "deleteForm" }))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.VentaID)

                <div class="form-actions">
                    <input type="submit" value="Eliminar" class="buttons danger" />
                    @Html.ActionLink("Cancelar", "ListarVentas", null, new { @class = "buttons info" })
                </div>
            }
        </div>
    </div>
</main>
