@model EFSRT_TORQUE.Models.Compras

@{
    ViewBag.Title = "Eliminar Compras";
}

<main>
    <div class="txtTittle">
        <p class="paddistance">Eliminar Compras</p>
        <ul>
            <li><ion-icon name="newspaper-outline"></ion-icon></li>
            <li><ion-icon name="notifications-outline"></ion-icon></li>
            <li><ion-icon name="person-outline"></ion-icon></li>
        </ul>
    </div>
    <div>
        <div class="paddistance bannermenu">
            <ul>
                <li><p>Compra a Eliminar</p></li>
            </ul>
        </div>
    </div>

    <div class="boxin">
        <div>
            <p>¿Estás seguro de que deseas eliminar este compra?</p>

            <dl style="display:flex;" class="texthei">
                <dt  class="sontext">
                    @Html.DisplayNameFor(model => model.CompraID)
                </dt>
                <dd class="sonbox">
                    @Html.DisplayFor(model => model.CompraID)
                </dd>
                <!-- Agrega más propiedades del producto si es necesario -->
            </dl>

            @using (Html.BeginForm("DeleteCompra", "Compras", FormMethod.Post, new { id = "deleteForm" }))
            {
                @Html.AntiForgeryToken()
                @Html.HiddenFor(model => model.CompraID)

                <div class="form-actions">
                    <input type="submit" value="Eliminar" class="buttons danger" />
                    @Html.ActionLink("Cancelar", "ListarCompras", null, new { @class = "buttons info" })
                </div>
            }
        </div>
    </div>
</main>
